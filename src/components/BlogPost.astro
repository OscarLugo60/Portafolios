---
const { title, url, image, description, pubDate, slug } = Astro.props
---

<article class="border border-[#027373] relative rounded-lg p-2 md:p-4 w-5/6 flex md:flex-row flex-col justify-center items-center gap-5">
    <img transition:name={slug} class="md:w-2/5 aspect-video md:aspect-auto rounded" src={image.url} alt={image.alt}>
    <div class="flex flex-col gap-y-2 h-full justify-between items-center w-full md:w-3/5">
        <a href={url}>
            <h2 class="text-md md:text-xl text-[#5FCDD9] font-semibold text-pretty">{title}</h2>
        </a>
        <p class="text-gray-500 text-sm text-pretty">{description}</p>
        <div class="flex gap-y-2 w-full mt-2 items-center justify-between">
            <p class="text-gray-500 text-xs w-auto">Publicado el: {pubDate.slice(0, 10)}</p>
            <a href={url} class="inline-flex items-center px-2 py-1 md:px-4 md:py-2 text-xs md:text-sm font-medium text-[#5FCDD9] bg-[#027373] border border-[#04BF9D] rounded-lg hover:bg-[#172026] hover:text-[#04BF9D] focus:z-10 focus:ring-4 focus:outline-none focus:ring-[#04BF9D] focus:text-[#04BF9D]  transition">Leer más</a>
        </div>
        
    </div>
</article>