---
const allPosts = await Astro.glob('../pages/posts/*.md');
const tags = [...new Set(allPosts.map((post) => post.frontmatter.tags).flat())];
---


<div class="flex flex-col w-1/6 fixed right-10 md:right-0 top-10 md:top-0 gap-3 justify-center items-center text-center px-5 md:px-0 my-10 md:my-28">
    <h3 class="text-md text-[#5FCDD9] font-semibold">Etiquetas:</h3>
    {tags.map((tag) => (
        <a class="text-sm text-[#5FCDD9] font-semibold border border-[#04BF9D] rounded-lg w-[120px] md:px-2 pb-2 pt-1 hover:bg-[#29353d] hover:text-[#04BF9D] transition capitalize" href={`/tags/${tag}`}>{tag}</a>
        )
    )}
</div>